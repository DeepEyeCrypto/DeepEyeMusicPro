<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/miniPlayerCard"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:cardBackgroundColor="@color/deep_surface_soft"
    app:cardCornerRadius="32dp"
    app:cardElevation="8dp"
    app:strokeWidth="1dp"
    app:strokeColor="#1AFFFFFF"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="10dp">

        <ImageView
            android:id="@+id/miniPlayerArtwork"
            android:layout_width="54dp"
            android:layout_height="54dp"
            android:background="@drawable/bg_artwork_rounded"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_notification"
            android:contentDescription="@string/app_name" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:id="@+id/miniPlayerTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:text="DeepEyeMusicPro"
                android:textColor="@color/deep_text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />
            <TextView
                android:id="@+id/miniPlayerSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:singleLine="true"
                android:text="Ready for premium playback"
                android:textColor="@color/deep_text_secondary"
                android:textSize="12sp" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/miniPlayPause"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_primary_button"
            android:src="@drawable/ic_play"
            android:contentDescription="@string/action_play" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
